.ftpquota
cgi-bin/
.well-known/
.DS_Store

# Internal / secret — never publish to this public repo
apps-script/
*.xlsx
*.pdf
*.pDF

# FTP is no longer primary — this reveals the old host IP/username publicly
deploy.sh
